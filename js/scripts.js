//business
function Album(arist, album, year, image) {
  this.artist = artistName;
  this.album = albumTitle;
  this.year = yearReleased;
  this.image = coverImage;
  this.compiler
}

//front end
$(document).ready(function(){
  $('#upload').click(function(event) {
    event.preventDefault();
    var inputName =
    var inputArtist =
    var inputYear =
    var inputImg =
    var albumItem = new Album(inputName, inputArtist, inputYear, inputImg);
  })
});
